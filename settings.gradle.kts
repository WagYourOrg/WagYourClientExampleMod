pluginManagement {
    repositories {
        mavenLocal()
        maven("https://maven.wagyourtail.xyz/releases")
        maven("https://maven.jemnetworks.com/snapshots")
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "WagYourClientExampleMod"
